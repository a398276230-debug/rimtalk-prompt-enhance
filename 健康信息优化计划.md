# 健康信息优化计划

## 背景
用户反馈：当一个角色身上有很多仿生体或伤口时，会全部发送给AI，导致tokens消耗过多。

## 解决方案
采用**分类过滤 + 智能整合**的组合方案。

---

## 1. Hediff 分类定义

| 类别 | 类型判断 | 说明 |
|------|----------|------|
| **仿生体/义肢** | `Hediff_AddedPart` | 替换身体部位的植入物 |
| **植入物** | `Hediff_Implant` 但非 `Hediff_AddedPart` | 不替换部位的植入物 |
| **伤口** | `Hediff_Injury` | 各种伤口 |
| **缺失部位** | `Hediff_MissingPart` | 缺失的身体部位 |
| **疾病/状态** | 其他 | 疾病、中毒、buff等 |

---

## 2. 新增设置项

在 `HealthEnhanceSettings.cs` 中添加：

```csharp
// === Health Filtering Settings ===

// 分类开关
public bool ShowBionics = true;           // 显示仿生体/义肢
public bool ShowImplants = true;          // 显示其他植入物
public bool ShowInjuries = true;          // 显示伤口
public bool ShowMissingParts = true;      // 显示缺失部位
public bool ShowConditions = true;        // 显示疾病/状态

// 数量限制 (0 = 无限制)
public int MaxBionicsToShow = 3;          // 最多显示仿生体数量（默认3）
public int MaxImplantsToShow = 3;         // 最多显示植入物数量（默认3）
public int MaxInjuriesToShow = 5;         // 最多显示伤口数量（默认5）
public int MaxConditionsToShow = 5;       // 最多显示状态数量（默认5）

// 智能整合选项
public bool EnableInjuryConsolidation = true;  // 启用伤口整合
public bool EnableBionicSummary = true;        // 启用仿生体摘要模式
public float MinorInjurySeverityThreshold = 0.3f; // 轻伤阈值
```

---

## 3. 智能整合逻辑

### 3.1 伤口整合规则

1. **按父部位整合**: 同一父部位下的多个子部位轻伤整合
   - 例: `左手食指擦伤 + 左手中指擦伤` → `Left hand: 2 minor wounds`

2. **只整合轻伤**: `severity < MinorInjurySeverityThreshold` 的伤口
   - 严重伤口仍然单独显示

3. **保留最严重的详情**: 整合后显示最严重的一个的信息

### 3.2 仿生体摘要模式

- 当仿生体数量 >= 3 且启用摘要模式时:
  - 显示: `Extensively enhanced (5 bionics: legs, arms, eye)`
- 否则正常列出每个仿生体

---

## 4. 输出格式对比

### 优化前：
```
Health:
- Bionic leg(Left leg), Severity:100%
- Bionic leg(Right leg), Severity:100%
- Bionic arm(Left arm), Severity:100%
- Bionic arm(Right arm), Severity:100%
- Bionic eye(Left eye), Severity:100%
- Scratch(Left hand index finger), Severity:15%, Pain:Mild
- Scratch(Left hand middle finger), Severity:12%, Pain:Mild
- Scratch(Left hand ring finger), Severity:10%, Pain:Mild
- Gunshot wound(Torso), Severity:65%, Pain:Severe, LETHAL
```

### 优化后：
```
Health:
- Extensively enhanced (5 bionics: both legs, both arms, left eye)
- Left hand: 3 minor scratches
- Gunshot wound(Torso), Severity:65%, Pain:Severe, LETHAL
```

---

## 5. 设置界面布局

```
🏥 健康信息增强
├── [现有设置...]
├── ━━ 分类过滤 ━━
│   ├── ☑ 显示仿生体/义肢    最多: [10] 个
│   ├── ☑ 显示其他植入物    最多: [10] 个
│   ├── ☑ 显示伤口          最多: [20] 个
│   ├── ☑ 显示缺失部位
│   └── ☑ 显示疾病/状态    最多: [10] 个
├── ━━ 智能整合 ━━
│   ├── ☑ 启用伤口整合
│   │   └── 轻伤阈值: [0.3] (低于此严重度整合)
│   └── ☑ 启用仿生体摘要模式
└── [提示: 设置为0表示无限制]
```

---

## 6. 实施步骤

### Step 1: 修改 HealthEnhanceSettings.cs
- 添加新设置字段
- 添加 ExposeData 保存/加载
- 修改 DrawHealthSection 添加UI控件

### Step 2: 修改 HealthInfoBuilder.cs
- 添加 hediff 分类方法
- 实现分类过滤逻辑
- 实现智能整合逻辑
- 实现仿生体摘要生成

### Step 3: 添加翻译
- 中文翻译 (ChineseSimplified/Keyed)
- 英文翻译 (English/Keyed)

### Step 4: 测试验证
- 测试各种组合场景
- 确保向后兼容

---

## 7. 翻译键值

```xml
<!-- 分类过滤 -->
<RTE_Settings_Health_FilterSection>分类过滤</RTE_Settings_Health_FilterSection>
<RTE_Settings_Health_ShowBionics>显示仿生体/义肢</RTE_Settings_Health_ShowBionics>
<RTE_Settings_Health_ShowImplants>显示其他植入物</RTE_Settings_Health_ShowImplants>
<RTE_Settings_Health_ShowInjuries>显示伤口</RTE_Settings_Health_ShowInjuries>
<RTE_Settings_Health_ShowMissingParts>显示缺失部位</RTE_Settings_Health_ShowMissingParts>
<RTE_Settings_Health_ShowConditions>显示疾病/状态</RTE_Settings_Health_ShowConditions>
<RTE_Settings_Health_MaxToShow>最多显示: {0} 个 (0=无限制)</RTE_Settings_Health_MaxToShow>

<!-- 智能整合 -->
<RTE_Settings_Health_ConsolidationSection>智能整合</RTE_Settings_Health_ConsolidationSection>
<RTE_Settings_Health_EnableInjuryConsolidation>启用伤口整合</RTE_Settings_Health_EnableInjuryConsolidation>
<RTE_Settings_Health_EnableInjuryConsolidation_Desc>将同一部位的轻伤合并显示</RTE_Settings_Health_EnableInjuryConsolidation_Desc>
<RTE_Settings_Health_EnableBionicSummary>启用仿生体摘要</RTE_Settings_Health_EnableBionicSummary>
<RTE_Settings_Health_EnableBionicSummary_Desc>3个以上仿生体时显示摘要而非逐一列出</RTE_Settings_Health_EnableBionicSummary_Desc>
<RTE_Settings_Health_MinorInjuryThreshold>轻伤阈值: {0:P0}</RTE_Settings_Health_MinorInjuryThreshold>
<RTE_Settings_Health_MinorInjuryThreshold_Desc>低于此严重度的伤口会被整合</RTE_Settings_Health_MinorInjuryThreshold_Desc>
```

---

## 8. 代码架构

```
HealthInfoBuilder.cs
├── BuildEnhancedHealthContext()        // 主入口
├── CategorizeHediffs()                 // 分类hediff
│   ├── IsBionic()                      // 判断仿生体
│   ├── IsImplant()                     // 判断植入物
│   ├── IsInjury()                      // 判断伤口
│   └── IsMissingPart()                 // 判断缺失
├── FilterHediffsByCategory()           // 按分类过滤
├── ConsolidateInjuries()               // 整合伤口
│   ├── GroupByParentPart()             // 按父部位分组
│   └── MergeMinorInjuries()            // 合并轻伤
├── GenerateBionicSummary()             // 生成仿生体摘要
└── FormatHediffInfo()                  // 格式化输出