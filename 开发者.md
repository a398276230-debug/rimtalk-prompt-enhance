# 开发者文档 - Patch对象列表

本项目使用Harmony库对 `RimTalk` Mod进行非破坏性增强。以下是所有被Patch的目标方法详情。

## 1. 健康信息增强 (Health Context)

- **目标类**: `RimTalk.Service.ContextBuilder`
- **目标方法**: `GetHealthContext`
- **Patch类型**: `Prefix` (前缀替换)
- **源文件**: `Source/HealthContextPatch.cs`
- **功能**: 拦截原方法的健康信息构建过程，替换为包含严重度、疼痛等级和详细描述的增强版本。

## 2. 装备信息增强 (Equipment Context)

- **目标类**: `RimTalk.Service.ContextBuilder`
- **目标方法**: `GetEquipmentContext`
- **Patch类型**: `Prefix` (前缀替换)
- **源文件**: `Source/EquipmentContextPatch.cs`
- **功能**: 拦截原方法的装备信息构建过程，替换为包含物品详细描述（武器、服装、携带物、背包）的增强版本。

## 技术细节

所有Patch都使用了 `Prefix` 方法并返回 `false`，这意味着原Mod的方法体会被完全跳过，由我们的增强逻辑接管。这确保了不会出现重复信息，同时也意味着我们需要完全负责该方法的原有功能（如基本的装备列表显示）。

### 依赖项
- `RimTalk.dll` (RimTalk Mod的核心程序集)
- `0Harmony.dll` (Harmony库)
