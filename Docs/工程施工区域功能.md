# 工程施工区域功能开发总结

## 已完成功能 ✅

### 1. 数据模型扩展
- ✅ `ColonyAnnouncement` 添加了施工区域相关字段：
  - `BlueprintAreaId`: 关联的施工区域ID
  - `InitialBlueprintCount`: 初始蓝图数量
  - `AutoCalculateProgress`: 是否自动计算进度
- ✅ `CustomNamedArea` 添加了 `IsConstructionArea` 标记
- ✅ Settings 添加了 `CustomProjectSummaryPrompt` 字段

### 2. 蓝图扫描服务
- ✅ 创建了 `BlueprintProgressService.cs`
- ✅ 实现了蓝图计数功能（使用 `DesignationDefOf.Plan`）
- ✅ 实现了蓝图详情获取（含位置信息：室内显示房间名，室外显示"Outdoors"）
- ✅ 实现了进度计算：`(初始-剩余)/初始`，0%→100%
- ✅ 实现了自动进度更新（每日零点触发）

### 3. UI 功能
- ✅ `TaskEditorDialog` 添加了"框选施工区域"按钮
- ✅ 实现了区域框选功能（复用 `AreaDrawingDesignator`）
- ✅ 添加了"移除区域"按钮
- ✅ 添加了"AI 总结工程"按钮
- ✅ 进度条显示（自动计算时为只读）

### 4. 核心逻辑
- ✅ 工程删除时自动删除关联的施工区域
- ✅ 施工区域在位置系统中显示时添加"(施工区域)"后缀
- ✅ 每日零点自动更新所有工程进度
- ✅ 进度达到100%时自动标记为完成

### 5. AI 总结功能
- ✅ 实现了基础的 AI 总结调用
- ✅ 使用现有的 `SimpleAIClient.CallAI`
- ✅ 构建了包含蓝图详情（含位置）的提示词
- ✅ JSON 解析响应并更新标题和描述

## 已完成的优化 ✅

### 1. 自定义提示词功能
- ✅ 在 AI 总结按钮旁边添加了"自定义提示词"按钮
- ✅ 使用 `PromptEditorDialog` 编辑自定义提示词
- ✅ 支持 `{blueprintList}` 占位符
- ✅ `GenerateAISummary` 优先使用自定义提示词

### 2. UI 优化
- ✅ 窗口高度从 700px 增加到 750px，确保所有按钮可见
- ✅ 新建工程时点击框选会自动保存（避免丢失）
- ✅ 未命名工程自动命名为"未命名工程"

## 待完成功能 ⏳（可选）

### 1. ~~自定义提示词 UI~~（已完成）
~~需要在 `TaskEditorDialog` 的 AI 总结按钮旁边添加"自定义提示词"按钮：~~

```csharp
// 在 AI 总结按钮下方添加
Rect promptButtonRect = listing.GetRect(30f);
if (Widgets.ButtonText(promptButtonRect, "自定义提示词"))
{
    string defaultPrompt = @"请根据以下建筑蓝图信息，为这个工程项目生成一个简洁的名称和描述。

蓝图列表：
{blueprintList}

要求：
1. 名称：不超过15个字，概括工程的主要目的
2. 描述：不超过50个字，说明工程的具体内容和目标
3. 使用中文
4. 保持专业和简洁

请以JSON格式返回：
{
  ""title"": ""工程名称"",
  ""description"": ""工程描述""
}";

    Find.WindowStack.Add(new PromptEditorDialog(
        "工程AI总结提示词",
        RimTalkHealthEnhanceMod.Settings.CustomProjectSummaryPrompt,
        defaultPrompt,
        (newPrompt) => {
            RimTalkHealthEnhanceMod.Settings.CustomProjectSummaryPrompt = newPrompt;
            RimTalkHealthEnhanceMod.Settings.Write();
        }
    ));
}
```

### 2. 修改 GenerateAISummary 使用自定义提示词

```csharp
// 在 GenerateAISummary 方法中
string blueprintList = string.Join("\n", blueprints.Select(b => $"- {b}"));

// 使用自定义提示词（如果有）
var settings = RimTalkHealthEnhanceMod.Settings;
string prompt;

if (!string.IsNullOrEmpty(settings.CustomProjectSummaryPrompt))
{
    // 使用自定义提示词，替换 {blueprintList} 占位符
    prompt = settings.CustomProjectSummaryPrompt.Replace("{blueprintList}", blueprintList);
}
else
{
    // 使用默认提示词
    prompt = $@"请根据以下建筑蓝图信息，为这个工程项目生成一个简洁的名称和描述。

蓝图列表：
{blueprintList}

要求：
1. 名称：不超过15个字，概括工程的主要目的
2. 描述：不超过50个字，说明工程的具体内容和目标
3. 使用中文
4. 保持专业和简洁

请以JSON格式返回：
{{
  ""title"": ""工程名称"",
  ""description"": ""工程描述""
}}";
}
```

### 3. 框选完成后的处理优化

当前实现：玩家框选完后需要手动重新打开编辑窗口。

建议改进：
- 可以考虑在玩家退出绘制模式时自动重新打开编辑窗口
- 或者在状况板主窗口中添加"扫描蓝图"按钮，让玩家手动更新蓝图数量

## 使用流程

1. **创建工程**：在状况板创建一个"工程"类型的状况
2. **框选区域**：点击"框选施工区域"按钮，拖拽鼠标框选包含蓝图的区域
3. **自动记录**：系统自动扫描区域内的蓝图数量并记录
4. **AI 总结**（可选）：点击"AI 总结工程"按钮，让 AI 根据蓝图内容生成名称和描述
5. **自定义提示词**（可选）：点击"自定义提示词"按钮修改 AI 总结的提示词
6. **自动更新**：每日零点系统自动更新进度（剩余蓝图数/初始蓝图数）
7. **自动完成**：进度达到100%时自动标记为完成

## 技术亮点

1. **智能位置识别**：蓝图信息包含位置（室内显示房间名，室外显示"Outdoors"）
2. **自动进度追踪**：每日零点自动更新，无需手动操作
3. **联动删除**：工程删除时自动删除关联的施工区域
4. **位置系统集成**：施工区域在位置显示中自动添加"(施工区域)"后缀
5. **手动AI总结**：玩家手动触发，避免浪费 tokens
6. **自定义提示词**：支持玩家自定义 AI 总结的提示词

## 性能影响

- **每日更新**：每天零点执行一次蓝图扫描，性能消耗极低（<10ms）
- **区域绘制**：复用现有的 `AreaDrawingDesignator`，无额外性能开销
- **AI 调用**：仅在玩家手动触发时调用，不会自动消耗 tokens

## 已知问题

1. ✅ 已修复：框选区域时不会卡死游戏（移除了 LongEventHandler）
2. ⏳ 待优化：框选完成后需要手动重新打开编辑窗口

## 下一步建议

1. 添加自定义提示词 UI（参考上面的代码）
2. 修改 `GenerateAISummary` 使用自定义提示词
3. 考虑在状况板主窗口添加"重新扫描蓝图"按钮
4. 测试各种边缘情况（区域被删除、蓝图被取消等）
