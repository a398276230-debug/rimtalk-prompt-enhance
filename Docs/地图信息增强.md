# 地图信息增强系统 - 设计文档

## 📋 需求概述

为 RimTalk 增强系统添加地图位置理解功能，让 AI 能够理解 Pawn 在殖民地中的相对位置，包括：
- 相对于殖民地中心的方位（东南西北）
- 所处区域类型（城镇核心/城镇/城镇边缘/野外）
- 所在房间信息（室内）或附近房间信息（室外）
- **所在 Area 信息**（种植区、储存区、自定义区域等）**[NEW]**

## 🎯 核心设计

### 1. 殖民地中心计算

**使用 Home Area（居住区）中心**，而非房间平均中心

**优势**：
- ✅ 玩家自定义，最准确反映"殖民地中心"
- ✅ 性能极佳，直接读取 `map.areaManager.Home.ActiveCells`
- ✅ 自动适应不规则形状的殖民地
- ✅ 自动排除远方孤立建筑（如避难所）

**计算方法**：
```csharp
private static IntVec3 CalculateHomeAreaCenter(Map map)
{
    var homeArea = map.areaManager.Home;
    if (homeArea == null || homeArea.TrueCount == 0)
        return map.Center;
    
    long sumX = 0, sumZ = 0;
    int count = 0;
    
    foreach (var cell in homeArea.ActiveCells)
    {
        sumX += cell.x;
        sumZ += cell.z;
        count++;
    }
    
    return count > 0 ? new IntVec3((int)(sumX / count), 0, (int)(sumZ / count)) : map.Center;
}
```

### 2. 区域类型判断

**基于居住区（Home Area）+ 周围8格检测**

**判断逻辑**：
1. **不在居住区** → `Wilderness`（野外）
2. **在居住区内**：
   - 周围8格都在居住区 → `Town`（城镇）
   - 周围8格都在居住区 + 距离中心 ≤ 20格 → `Town Center`（城镇核心，可选）
   - 周围5-7格在居住区 → `Town`（城镇）
   - 周围1-4格在居住区 → `Town Edge`（城镇边缘）

**代码实现**：
```csharp
private static string GetTownZone(Pawn pawn, IntVec3 position)
{
    var homeArea = pawn.Map.areaManager.Home;
    
    if (!homeArea[position])
        return "Wilderness";
    
    // 检测周围8格
    int neighborCount = 0;
    foreach (var offset in GenAdj.AdjacentCells)
    {
        IntVec3 neighbor = position + offset;
        if (neighbor.InBounds(pawn.Map) && homeArea[neighbor])
            neighborCount++;
    }
    
    // 8格都在居住区
    if (neighborCount == 8)
    {
        // 可选：检测是否是核心区
        if (settings.EnableTownCenterDetection)
        {
            float distanceToCenter = position.DistanceTo(_homeAreaCenter);
            if (distanceToCenter <= settings.TownCenterRadius)
                return "Town Center";
        }
        return "Town";
    }
    
    // 5-7格在居住区
    if (neighborCount >= 5)
        return "Town";
    
    // 1-4格在居住区 = 边缘
    return "Town Edge";
}
```

### 3. 方位判断

**8方位系统**：North, South, East, West, Northeast, Northwest, Southeast, Southwest, Center

**判断逻辑**：
```csharp
private static string GetRelativeDirection(IntVec3 position, IntVec3 center)
{
    if (!center.IsValid) return "";
    
    int dx = position.x - center.x;
    int dz = position.z - center.z;
    
    // 非常接近中心（5格内）
    if (Mathf.Abs(dx) < 5 && Mathf.Abs(dz) < 5)
        return "Center";
    
    // 8方位判断
    if (Mathf.Abs(dx) > Mathf.Abs(dz) * 2)
        return dx > 0 ? "East" : "West";
    else if (Mathf.Abs(dz) > Mathf.Abs(dx) * 2)
        return dz > 0 ? "North" : "South";
    else if (dx > 0 && dz > 0) return "Northeast";
    else if (dx > 0 && dz < 0) return "Southeast";
    else if (dx < 0 && dz > 0) return "Northwest";
    else return "Southwest";
}
```

### 4. Area（区域）检测 **[NEW]**

**核心思路**：
- **复制"活动区"（Allowed Area）的机制**，但**仅用于 AI 读取，不影响游戏显示**
- 玩家可以创建多个命名区域（如"训练场"、"工业区"、"农场"等）
- 这些区域**不会出现在游戏的各种 UI 中**，避免污染界面
- **只在 AI 对话时读取**，让 AI 知道 Pawn 在哪个功能区域

**读取游戏原生 Area**：
- Growing Zone（种植区）
- Storage Zone（储存区）
- Dumping Stockpile（垃圾堆）
- Animal Area（动物区域）
- 等等...

**自定义命名 Area 系统**：
- 完全独立的 Area 系统，不与游戏原生 Area 混合
- 玩家在我们的 UI 中创建、绘制、命名区域
- 数据保存在 `ColonyAnnouncementManager` 中
- 优先级：自定义命名 Area > 原版功能性 Area > 无

**检测逻辑**：
```csharp
private static string GetAreaInfo(Pawn pawn, IntVec3 position)
{
    var map = pawn.Map;
    var areaManager = map.areaManager;
    
    // 1. 检测自定义命名区域（优先级最高）
    var customArea = GetCustomAreaAt(map, position);
    if (customArea != null)
        return $"in {customArea.Label}";
    
    // 2. 检测原版功能性区域
    // 种植区
    foreach (var zone in map.zoneManager.AllZones)
    {
        if (zone is Zone_Growing growZone && growZone.ContainsCell(position))
            return "in Growing Zone";
    }
    
    // 储存区
    foreach (var zone in map.zoneManager.AllZones)
    {
        if (zone is Zone_Stockpile stockpile && stockpile.ContainsCell(position))
        {
            string label = stockpile.label ?? "Storage Zone";
            return $"in {label}";
        }
    }
    
    // 3. 检测动物区域
    if (pawn.playerSettings?.AreaRestrictionInPawnCurrentMap != null)
    {
        var animalArea = pawn.playerSettings.AreaRestrictionInPawnCurrentMap;
        if (animalArea[position])
            return $"in {animalArea.Label}";
    }
    
    return null; // 不在任何特殊区域
}
```

**自定义 Area 数据结构**：
```csharp
// 新增数据模型 - 完全独立的 Area 系统
public class CustomNamedArea : IExposable
{
    public string Id;                    // 唯一标识
    public string Label;                 // 玩家自定义名称
    public BoolGrid Cells;               // 区域格子（类似 Area 的实现）
    public Color Color = Color.white;    // 显示颜色
    public bool IsActive = true;         // 是否启用
    
    private Map map;
    
    public CustomNamedArea()
    {
    }
    
    public CustomNamedArea(Map map, string label)
    {
        this.map = map;
        this.Id = Guid.NewGuid().ToString();
        this.Label = label;
        this.Cells = new BoolGrid(map);
    }
    
    // 检测某个位置是否在区域内
    public bool this[IntVec3 c]
    {
        get => Cells != null && Cells[c];
        set
        {
            if (Cells != null)
                Cells[c] = value;
        }
    }
    
    // 获取区域内的所有格子
    public IEnumerable<IntVec3> ActiveCells
    {
        get
        {
            if (Cells == null || map == null) yield break;
            
            foreach (var cell in map.AllCells)
            {
                if (Cells[cell])
                    yield return cell;
            }
        }
    }
    
    public void ExposeData()
    {
        Scribe_Values.Look(ref Id, "id");
        Scribe_Values.Look(ref Label, "label");
        Scribe_Values.Look(ref Color, "color", Color.white);
        Scribe_Values.Look(ref IsActive, "isActive", true);
        Scribe_Deep.Look(ref Cells, "cells");
    }
}

// 在 ColonyAnnouncementManager 中管理
public class ColonyAnnouncementManager : GameComponent
{
    public List<CustomNamedArea> CustomAreas = new List<CustomNamedArea>();
    
    // ... 现有代码 ...
}
```

**UI 管理界面**：
- 在通告板窗口添加"自定义区域"标签页
- 列出所有自定义区域
- 支持创建、编辑、删除、重命名区域
- **提供绘制工具**（类似游戏原生的 Area 绘制）：
  - 点击"绘制"按钮进入绘制模式
  - 使用鼠标拖拽选择区域
  - 支持添加/移除格子
  - 支持填充/清空整个区域

### 5. 室内/室外场景

**室内场景**：
- 读取房间名称（`room.Role?.label`）
- 输出格式：`In [RoomName], [Direction] of colony ([Zone])`

**室外场景**：
- 检测5格范围内是否有房间
- 有房间：`Outdoors near [RoomName] ([Distance] cells), [Direction] of colony ([Zone])`
- 无房间：`Outdoors, [Direction] of colony ([Zone])`

**附近房间检测**：
```csharp
private static string GetNearbyRoomName(Pawn pawn, int range)
{
    var map = pawn.Map;
    
    for (int radius = 1; radius <= range; radius++)
    {
        foreach (var cell in GenRadial.RadialCellsAround(pawn.Position, radius, true))
        {
            if (!cell.InBounds(map)) continue;
            
            var room = cell.GetRoom(map);
            if (room != null && !room.PsychologicallyOutdoors && room.Role != null)
            {
                return $"{room.Role.label} ({radius} cells)";
            }
        }
    }
    
    return null;
}
```

## 🔧 性能优化

### 缓存机制

**跟随 RimTalk 的 TalkInterval**（默认7秒）更新中心点：

```csharp
private static IntVec3 _homeAreaCenter = IntVec3.Invalid;
private static int _lastUpdateTick = 0;
private const int UPDATE_INTERVAL = 420; // 7秒 = 420 ticks

private static void UpdateHomeAreaCenterIfNeeded(Map map)
{
    int currentTick = Find.TickManager.TicksGame;
    
    if (currentTick - _lastUpdateTick < UPDATE_INTERVAL)
        return;
    
    _homeAreaCenter = CalculateHomeAreaCenter(map);
    _lastUpdateTick = currentTick;
}
```

### 线程安全

- 所有缓存变量使用 `static` 字段
- 只在主线程（GameComponentTick 或 Patch）中更新
- 读取操作无需锁（单次赋值的原子性）

## 📊 输出示例

### 场景1：在城镇核心的卧室
```
In Bedroom, Northeast of colony (Town Center)
```

### 场景2：在城镇内的工坊
```
In Workshop, East of colony (Town)
```

### 场景3：在城镇边缘的墙壁附近
```
Outdoors near Wall (2 cells), South of colony (Town Edge)
```

### 场景4：在野外
```
Outdoors, North of colony (Wilderness)
```

### 场景5：在远方避难所（未标记为居住区）
```
In Emergency Shelter, Far North of colony (Wilderness)
```

### 场景6：在种植区工作 **[NEW]**
```
Outdoors in Growing Zone, South of colony (Town)
```

### 场景7：在自定义区域"训练场" **[NEW]**
```
Outdoors in Training Ground, East of colony (Town Edge)
```

### 场景8：在储存区 **[NEW]**
```
In Warehouse (Storage Zone), West of colony (Town)
```

## 🏗️ 技术实现

### 文件结构

```
Source/
├── Models/
│   ├── ColonyAnnouncementManager.cs # 修改：添加 CustomAreas 管理
│   └── CustomNamedArea.cs           # 新增：自定义区域数据模型
├── Services/
│   └── LocationContextBuilder.cs    # 新增：位置上下文构建服务（含 Area 检测）
├── Patches/
│   └── LocationContextPatch.cs      # 新增：拦截 ContextBuilder.BuildLocationContext
├── UI/
│   └── MainTabWindow_Announcement.cs # 修改：添加"自定义区域"标签页
└── Settings/
    └── HealthEnhanceSettings.cs     # 修改：添加位置相关配置
```

### 集成方式

**方案A：Patch ContextBuilder（推荐）**

拦截 `ContextBuilder.BuildLocationContext` 方法，在现有位置信息后追加：

```csharp
[HarmonyPatch(typeof(ContextBuilder), "BuildLocationContext")]
public static class LocationContextPatch
{
    static void Postfix(StringBuilder sb, Pawn mainPawn)
    {
        var settings = RimTalkHealthEnhanceMod.Settings;
        if (!settings.ShowRelativeLocation) return;
        
        string relativeLocation = LocationContextBuilder.GetRelativeLocation(mainPawn);
        if (!string.IsNullOrEmpty(relativeLocation))
        {
            sb.Append($"\n{relativeLocation}");
        }
    }
}
```

**方案B：独立注入到 AnnouncementContext**

如果需要全局位置信息（多人对话场景），可在 `AnnouncementBuilder` 中添加。

### 配置选项

```csharp
// 在 HealthEnhanceSettings.cs 中添加
public bool ShowRelativeLocation = true;           // 启用相对位置显示
public bool ShowAreaInfo = true;                   // 显示 Area 信息 [NEW]
public bool EnableTownCenterDetection = false;     // 启用城镇核心检测（可选）
public int TownCenterRadius = 20;                  // 城镇核心半径
public int NearbyRoomRange = 5;                    // 附近房间检测范围
```

### UI 设置界面

在设置界面添加新的标签页或在现有"通告"标签页中添加：

```csharp
public void DoLocationSettingsWindowContents(Rect inRect)
{
    Listing_Standard listing = new Listing_Standard();
    listing.Begin(inRect);
    
    listing.CheckboxLabeled("启用相对位置显示", ref ShowRelativeLocation, 
        "显示 Pawn 相对于殖民地中心的位置和方位");
    
    if (ShowRelativeLocation)
    {
        listing.CheckboxLabeled("  └─ 启用城镇核心检测", ref EnableTownCenterDetection,
            "区分城镇核心和普通城镇区域");
        
        if (EnableTownCenterDetection)
        {
            Widgets.Label(listing.GetRect(22f), $"    └─ 核心区半径: {TownCenterRadius} 格");
            TownCenterRadius = (int)listing.Slider(TownCenterRadius, 10, 50);
        }
        
        Widgets.Label(listing.GetRect(22f), $"附近房间检测范围: {NearbyRoomRange} 格");
        NearbyRoomRange = (int)listing.Slider(NearbyRoomRange, 3, 10);
    }
    
    listing.End();
}
```

## ⚠️ 注意事项

### 1. 地图切换
需要在地图切换时清空缓存：
```csharp
public static void OnMapChanged()
{
    _homeAreaCenter = IntVec3.Invalid;
    _lastUpdateTick = 0;
}
```

### 2. 无居住区情况
如果玩家未设置居住区，回退到地图中心：
```csharp
if (homeArea == null || homeArea.TrueCount == 0)
    return map.Center;
```

### 3. 多地图支持
确保在非主殖民地地图上也能正常工作（使用当前地图的居住区）。

### 4. 性能监控
- Home Area 遍历在大型殖民地（10000+格）可能耗时
- 可选：采样计算（每10格采样1格）

## 🎯 实现步骤

### 阶段1：基础位置系统
1. **创建 LocationContextBuilder.cs** - 核心逻辑服务（方位、区域判断）
2. **修改 HealthEnhanceSettings.cs** - 添加配置选项和 UI
3. **创建 LocationContextPatch.cs** - Harmony 补丁
4. **测试验证** - 基础功能测试

### 阶段2：Area 检测系统 **[NEW]**
1. **扩展 LocationContextBuilder.cs** - 添加原版 Area 检测
2. **创建 CustomNamedArea.cs** - 自定义区域数据模型
3. **修改 ColonyAnnouncementManager.cs** - 添加 CustomAreas 管理
4. **扩展 MainTabWindow_Announcement.cs** - 添加"自定义区域"标签页
5. **实现 UI 功能**：
   - 列表显示所有自定义区域
   - 创建新区域（选择游戏中的 Area + 输入名称）
   - 编辑/删除区域
   - 颜色选择器
6. **测试验证** - Area 检测和自定义区域功能

### 阶段3：优化与完善
1. **性能优化** - 确保不影响游戏帧率
2. **更新文档** - 更新项目总结.md
3. **用户测试** - 收集反馈并调整

## 📝 待确认事项

1. **是否需要"Town Center"分级？**
   - 需要：4级分类（Town Center / Town / Town Edge / Wilderness）
   - 不需要：3级分类（Town / Town Edge / Wilderness）

2. **附近房间检测范围？**
   - 当前设计：5格
   - 可配置：3-10格

3. **集成方式？**
   - 方案A：Patch ContextBuilder（推荐，与现有位置信息整合）
   - 方案B：独立注入到 AnnouncementContext（全局信息）

4. **是否需要缓存 Pawn 位置信息？**
   - 当前设计：每次调用实时计算（性能足够）
   - 可选：缓存每个 Pawn 的位置字符串（更复杂）

5. **自定义 Area 的创建方式？** **[NEW]**
   - ✅ **完全独立的 Area 系统**（类似"活动区"）：
     - 在我们的 UI 中创建和绘制区域
     - 使用 `BoolGrid` 存储区域数据（与游戏原生 Area 相同的底层结构）
     - **不出现在游戏的任何原生 UI 中**，避免界面污染
     - 优势：完全可控、不影响游戏体验、专门为 AI 服务

6. **Area 优先级？** **[NEW]**
   - 当前设计：自定义 Area > 原版功能性 Area（种植区/储存区）> 无
   - 如果一个位置同时在多个自定义 Area 中，显示最近创建的？

7. **是否显示原版 Area 的默认名称？** **[NEW]**
   - 种植区：显示为 "Growing Zone" 还是跳过？
   - 储存区：显示储存区的自定义标签（如果有）
   - **建议**：都显示，让 AI 知道 Pawn 在做什么

## 🚀 预期效果

AI 将能够理解：
- "我在城镇东北方的卧室里"
- "我在城镇边缘，靠近工坊"
- "我在野外，殖民地北方"
- **"我在种植区工作"** **[NEW]**
- **"我在训练场（玩家自定义区域）"** **[NEW]**
- **"我在仓库的储存区"** **[NEW]**

这将显著提升 AI 对话的沉浸感和情境感知能力！

## 🎨 自定义 Area UI 设计 **[NEW]**


### 技术实现要点

1. **独立的 Area 系统实现**：
   - 使用 `BoolGrid` 存储区域数据（与游戏原生 Area 相同）
   - 完全不依赖游戏的 `AreaManager`
   - 数据保存在 `ColonyAnnouncementManager` 中
   
2. **绘制工具实现**：
   ```csharp
   // 参考游戏原生的 Designator_AreaAllowed 实现
   public class AreaDrawingTool
   {
       private CustomNamedArea currentArea;
       private bool isDrawing = false;
       private bool isAdding = true;  // true=添加格子, false=移除格子
       
       public void StartDrawing(CustomNamedArea area, bool adding)
       {
           currentArea = area;
           isDrawing = true;
           isAdding = adding;
       }
       
       public void ProcessInput()
       {
           if (!isDrawing) return;
           
           // 获取鼠标位置
           IntVec3 cell = UI.MouseCell();
           if (!cell.InBounds(Find.CurrentMap)) return;
           
           // 左键拖拽选择区域
           if (Input.GetMouseButton(0))
           {
               currentArea[cell] = isAdding;
           }
           
           // 右键或ESC退出绘制模式
           if (Input.GetMouseButtonDown(1) || Input.GetKeyDown(KeyCode.Escape))
           {
               isDrawing = false;
           }
       }
       
       public void DrawOverlay()
       {
           if (!isDrawing || currentArea == null) return;
           
           // 绘制区域高亮
           foreach (var cell in currentArea.ActiveCells)
           {
               CellRenderer.RenderCell(cell, 0.5f, currentArea.Color);
           }
       }
   }
   ```

3. **UI 界面实现**：
   ```csharp
   // 在 MainTabWindow_Announcement 中添加新标签页
   private void DrawCustomAreasTab(Rect inRect)
   {
       var listing = new Listing_Standard();
       listing.Begin(inRect);
       
       // 区域列表
       foreach (var area in manager.CustomAreas)
       {
           Rect row = listing.GetRect(30f);
           
           // 颜色指示器
           Widgets.DrawBoxSolid(row.LeftPartPixels(20f), area.Color);
           
           // 区域名称
           Widgets.Label(row.LeftPartPixels(200f).RightPartPixels(180f), area.Label);
           
           // 操作按钮
           if (Widgets.ButtonText(row.RightPartPixels(150f).LeftPartPixels(50f), "绘制"))
           {
               StartDrawingArea(area);
           }
           if (Widgets.ButtonText(row.RightPartPixels(90f).LeftPartPixels(40f), "编辑"))
           {
               OpenEditDialog(area);
           }
           if (Widgets.ButtonText(row.RightPartPixels(40f), "删除"))
           {
               manager.CustomAreas.Remove(area);
               break;
           }
       }
       
       listing.Gap();
       
       // 创建新区域按钮
       if (listing.ButtonText("+ 创建新区域"))
       {
           var newArea = new CustomNamedArea(Find.CurrentMap, "新区域");
           manager.CustomAreas.Add(newArea);
           StartDrawingArea(newArea);
       }
       
       listing.End();
   }
   ```

4. **检测逻辑**：
   ```csharp
   private static CustomNamedArea GetCustomAreaAt(Map map, IntVec3 position)
   {
       var manager = ColonyAnnouncementManager.Instance;
       if (manager?.CustomAreas == null) return null;
       
       // 遍历所有自定义区域（优先级：最后创建的优先）
       for (int i = manager.CustomAreas.Count - 1; i >= 0; i--)
       {
           var customArea = manager.CustomAreas[i];
           if (!customArea.IsActive) continue;
           if (customArea.Cells == null) continue;
           
           if (customArea[position])
               return customArea;
       }
       
       return null;
   }
   ```

5. **数据持久化**：
   ```csharp
   // 在 ColonyAnnouncementManager 中
   public override void ExposeData()
   {
       base.ExposeData();
       Scribe_Collections.Look(ref CustomAreas, "customAreas", LookMode.Deep);
       
       // 初始化
       if (CustomAreas == null)
           CustomAreas = new List<CustomNamedArea>();
       
       // 加载后重新关联 Map
       if (Scribe.mode == LoadSaveMode.PostLoadInit)
       {
           var map = Find.CurrentMap;
           foreach (var area in CustomAreas)
           {
               // 重新初始化 BoolGrid（如果需要）
               if (area.Cells == null && map != null)
                   area.Cells = new BoolGrid(map);
           }
       }
   }
   ```

6. **性能优化**：
   - Area 检测本身很快（`Area[position]` 是 O(1) 操作）
   - 自定义区域数量通常不多（<20个），遍历开销可忽略
   - 缓存 Area 引用可进一步优化（可选）

7. **用户体验优化**：
   - 绘制时实时显示区域高亮
   - 支持快捷键（如 Ctrl+Z 撤销）
   - 显示区域统计信息（格子数量、覆盖面积）
   - 支持复制/粘贴区域
   - 支持导入/导出区域数据
